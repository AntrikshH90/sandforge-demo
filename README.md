# sandforge-demo

Deliberately broken fibonacci implementation. Target repo for the
[SandForge](https://github.com/AntrikshH90/sandforge) agent demo.

The bug: `fib(0)` returns `1` instead of `0` (base case guard is `n < 1`).
